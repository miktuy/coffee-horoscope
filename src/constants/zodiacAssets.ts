/**
 * Bump this when you replace files under `public/images/zodiac/`.
 * Appends `?v=…` to image URLs so `next/image` and the browser fetch fresh PNGs.
 */
export const ZODIAC_IMAGE_CACHE_REVISION = "2";
